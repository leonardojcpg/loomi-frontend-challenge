import styled from 'styled-components';

export const CornerPageBackground = styled.div`
    height: 100vh;
    display: flex;
    position: relative;
`;

export const CornerLeft = styled.div`
    position: fixed;
    top: 0;
    left: 0;
`;

export const CornerRight = styled.div`
    margin: -0.5rem;
    position: fixed;
    bottom: 0;
    right: 0;
`;
